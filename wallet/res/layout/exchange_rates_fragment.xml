<?xml version="1.0" encoding="utf-8"?>
<ViewAnimator xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/exchange_rates_list_group"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:id="@+id/select_currency_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/select_currency"
            app:layout_constraintTop_toTopOf="@+id/close_exchange_rate_btn"
            app:layout_constraintBottom_toBottomOf="@+id/close_exchange_rate_btn"
            app:layout_constraintStart_toStartOf="@id/left_guideline"
            style="@style/MontserratSemiBold"
            android:textColor="@color/gray_900"
            android:textSize="15sp"
            />

        <ImageView
            android:id="@+id/close_exchange_rate_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="20dp"
            app:layout_constraintEnd_toEndOf="@id/right_guideline"
            android:background="@drawable/close_exchange_rates_view"
            />

        <LinearLayout
            android:id="@+id/search_exchange_rate_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="@drawable/rounded_corners_bgd_light_gray"
            app:layout_constraintStart_toStartOf="@id/left_guideline"
            app:layout_constraintEnd_toEndOf="@id/right_guideline"
            android:layout_marginTop="16dp"
            android:paddingTop="12dp"
            android:paddingBottom="12dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            app:layout_constraintTop_toBottomOf="@id/close_exchange_rate_btn">

            <EditText
                android:id="@+id/search_exchange_rate_btn"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:drawablePadding="12dp"
                android:hint="@android:string/search_go"
                android:textSize="15sp"
                style="@style/MontserratRegular"
                android:background="@null"
                app:drawableLeftCompat="@drawable/ic_search_light_gray"
                />
        </LinearLayout>

        <ProgressBar
            android:id="@+id/exchange_rates_loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_gravity="center"
            android:visibility="gone" />

        <TextView
            android:id="@+id/exchange_rates_loading_error"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="@string/exchange_rates_fragment_empty_text"
            android:textColor="@color/fg_less_significant"
            android:textSize="@dimen/font_size_small"
            android:visibility="gone" />

        <TextView
            android:id="@+id/exchange_rates_empty_search"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="@string/exchange_rates_fragment_empty_search"
            android:textColor="@color/fg_less_significant"
            android:textSize="@dimen/font_size_small"
            android:visibility="gone" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/exchange_rates_list"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            app:layout_constraintTop_toBottomOf="@id/search_exchange_rate_container"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginTop="10dp"
            android:clipToPadding="false"
            android:paddingTop="10dp"
            android:paddingBottom="10dp"
            android:scrollbars="vertical"
            tools:visibility="visible"
            android:visibility="gone" />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/left_guideline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintGuide_begin="15dp"
            android:orientation="vertical"
            />

        <androidx.constraintlayout.widget.Guideline
            android:id="@+id/right_guideline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintGuide_end="15dp"
            android:orientation="vertical"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>

</ViewAnimator>