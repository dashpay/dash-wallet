<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/list_view_btn_ripple"
    xmlns:tools="http://schemas.android.com/tools">

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="10dp" tools:radius="10dp" />
            <solid android:color="@android:color/white" tools:color="@android:color/white" />
            <stroke android:color="@android:color/white" android:width="2dp" tools:color="@android:color/white"/>
        </shape>
    </item>
</ripple>